# Poke.AI integration (https://poke.com)
# Create an API key at https://poke.com/settings/advanced
POKE_API_KEY=
# Optional: iMessage contact name to read Poke reply from (default: Poke)
# POKE_MESSAGES_CONTACT=Poke
# Optional: poll interval (ms) and timeout (ms) when waiting for reply in Messages
# POKE_POLL_INTERVAL_MS=5000
# POKE_POLL_TIMEOUT_MS=90000
# POKE_FIRST_POLL_DELAY_MS=5000
# Optional: for verifying Poke outbound webhook signatures (future use)
POKE_WEBHOOK_SECRET=
